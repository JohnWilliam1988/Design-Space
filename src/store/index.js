import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

const store = new Vuex.Store({
  state: {
    layer: ["fasdfasdf"],
  },
  getters: {},
  actions: {},
  mutations: {},
});

export default store;
